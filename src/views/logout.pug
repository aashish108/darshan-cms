html
  include includes/head.pug
  body.page-uploadRawImages
    section.hero.is-fullheight
      .hero-head
        include includes/header.pug
      .hero-body
        .container.is-fluid
          .columns
            include includes/adminSidebar.pug
            form(action="/darshan-app/login" method="post" enctype="multipart/form-data")
              .column
                .columns
                  .column.is-vcentered
                    input.input(type='text' placeholder='Username')
                .columns.is-vcentered
                  .column
                    input.input(type='password' placeholder='Password')
                    button.button.is-success(type='submit') Submit
      
    include includes/footerScripts.pug
    script(defer='' src='/darshan-app/public/js/uploading-raw-files.js')